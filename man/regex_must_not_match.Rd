% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regex.R
\name{regex_must_not_match}
\alias{regex_must_not_match}
\title{Create a 'must not match' regex rule}
\usage{
regex_must_not_match(regex)
}
\arguments{
\item{regex}{\code{(character)} The regular expression pattern.}
}
\value{
The \code{regex} value with a \code{negate} attribute and with \code{\link[=names]{names()}} equal
to the generated "must not match" error message.
}
\description{
Attach a standardized error message to a \code{regex} argument that specifies
that the pattern must \emph{not} be matched. This is a wrapper around
\code{\link[=regex_must_match]{regex_must_match()}} that sets the \code{negate} attribute to \code{TRUE}.
}
\examples{
regex_must_not_match("[aeiou]")
}
